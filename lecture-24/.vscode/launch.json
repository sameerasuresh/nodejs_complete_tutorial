{
    "configurations": [
        {
            "command": "npm run debug",
            "name": "Debug App",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}