{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Debug App",
            "request": "launch",
            "command": "npm run debug",
            "cwd": "${workspaceFolder}",
        }
    ]
}